-- stockmd-mini local seed. Synthetic data only — never production rows.
-- Schemas live in supabase/migrations/. This file is rerun on every `seed.sh`.

-- (placeholder) The bootstrap leaves seeding empty. As each resource is
-- implemented, append minimal INSERT statements here so the verifier and
-- developer can exercise the app without manual data entry.

-- TODO: seed `stocks` once its table is created in supabase/migrations/.
-- TODO: seed `lots` once its table is created in supabase/migrations/.
-- TODO: seed `suppliers` once its table is created in supabase/migrations/.
-- TODO: seed `purchase_orders` once its table is created in supabase/migrations/.
-- TODO: seed `nfc_tags` once its table is created in supabase/migrations/.

